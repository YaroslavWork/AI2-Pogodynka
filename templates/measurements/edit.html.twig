{% extends 'base.html.twig' %}

{% block title %}Edit Weather{% endblock %}

{% block body %}
    <div class="btn-group">
        <h1 class="text-muted">Weather</h1><h1 class="text-primary">></h1><h1>Edit</h1>
    </div>

    {{ include('measurements/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="container text-center">
        <div class="btn-group">
            <a href="{{ path('app_measurements_index') }}"><button type="button" class="btn btn-outline-secondary m-1">Back to list</button></a>
            {{ include('measurements/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
