{% extends 'base.html.twig' %}

{% block title %}New Weather{% endblock %}

{% block body %}
    <div class="btn-group">
        <h1 class="text-muted">Weather</h1><h1 class="text-primary">></h1><h1>New</h1>
    </div>

    {{ include('measurements/_form.html.twig') }}

    <div class="container text-center">
        <a href="{{ path('app_measurements_index') }}"><button class="btn btn-outline-secondary">Back to list</button></a>
    </div>
{% endblock %}
